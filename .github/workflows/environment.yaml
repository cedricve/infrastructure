name: GitHub Actions Environment
on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "v*"
      - "release/*"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
